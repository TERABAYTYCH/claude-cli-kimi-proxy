Выполни задачу строго по шагам, в указанном порядке. Ничего не пропускай,
ничего не добавляй от себя, не предлагай альтернативы.

ОГРАНИЧЕНИЯ (важны для чистоты замера):
- Не используй субагентов и параллельные swarm-запуски. Всю работу делай сам.
- Не читай файлов и не запускай команд сверх перечисленных ниже.
- Если команда или инструмент недоступны в твоём окружении — НЕ ищи обходных
  путей, не пробуй абсолютные пути и другие способы запуска. Один раз запиши
  для этого шага NOT_AVAILABLE и переходи к следующему.
- Рабочий каталог: /root/work/claude-max-api-proxy
- Файлы проекта не изменяй. Писать можно только в /tmp/benchmark-workspace.

ШАГ 1. Подготовь рабочий каталог:
  rm -rf /tmp/benchmark-workspace && mkdir -p /tmp/benchmark-workspace

ШАГ 2. Найди по маске все файлы *.ts внутри src/ и сообщи их количество.

ШАГ 3. Прочитай целиком эти пять файлов:
  src/server/routes.ts
  src/adapter/openai-to-cli.ts
  src/adapter/delegate-parser.ts
  src/adapter/cli-to-openai.ts
  src/subprocess/manager.ts

ШАГ 4. Выполни эти три поиска по проекту:
  cache_read_input_tokens
  acquireSessionWait
  DELEGATION_INSTRUCTION_END

ШАГ 5. Выполни эти три команды:
  git log --oneline -5
  npx tsc --noEmit && echo TSC_OK
  find src -name '*.ts' | xargs wc -l | tail -1

═══ ГЕЙТ 1 ═══
Спроси меня, в каком порядке выстроить разделы итогового отчёта: "по слоям"
или "по файлам". ОСТАНОВИСЬ и дождись моего ответа. Не предполагай ответ и
не продолжай без него. Дальше следуй ровно тому, что я скажу.

ШАГ 6. Прочитай целиком ещё четыре файла:
  src/subprocess/session-store.ts
  src/types/openai.ts
  src/types/claude-cli.ts
  src/utils/logger.ts

ШАГ 7. Выполни эти две команды:
  git diff --stat HEAD~3 HEAD
  ls -la tests/integration

ШАГ 8. Создай файл /tmp/benchmark-workspace/report.md со сводкой на
150-250 слов: что за проект, из каких слоёв состоит, как данные идут от
входящего HTTP-запроса до ответа.

ШАГ 9. Допиши в конец того же файла строку вида:
  FILES_READ=9 SEARCHES=3 COMMANDS=5

ШАГ 10. Прочитай /tmp/benchmark-workspace/report.md обратно и убедись, что
обе части на месте.

═══ ГЕЙТ 2 ═══
Спроси у меня разрешение удалить /tmp/benchmark-workspace. ОСТАНОВИСЬ и
дождись ответа. Удаляй только если я разрешу.

ШАГ 11. Составь итоговый отчёт в том порядке разделов, который я выбрал в
ГЕЙТЕ 1. Он должен содержать:
  - назначение проекта в двух-трёх предложениях;
  - путь запроса от приёма до ответа с указанием файлов;
  - механику сессий и делегирования;
  - число совпадений по каждому из трёх поисков шага 4;
  - прошёл ли tsc и суммарное число строк в src;
  - что было записано в report.md.

Отчёт — твой финальный ответ. Новых файлов не создавай.
