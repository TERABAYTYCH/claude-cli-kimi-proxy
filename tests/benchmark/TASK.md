Выполни задачу строго по шагам, в указанном порядке. Ничего не пропускай,
ничего не добавляй от себя, не предлагай альтернативы и не спрашивай
уточнений — всё необходимое указано ниже.

ОГРАНИЧЕНИЯ (важны для чистоты замера):
- Не используй субагентов и параллельные swarm-запуски. Всю работу делай сам.
- Не читай файлы, которых нет в списке ниже.
- Не запускай команд, которых нет в списке ниже.
- Рабочий каталог: /root/work/claude-max-api-proxy

ШАГ 1. Прочитай целиком эти пять файлов:
  package.json
  src/types/openai.ts
  src/adapter/openai-to-cli.ts
  src/adapter/delegate-parser.ts
  src/subprocess/session-store.ts

ШАГ 2. Выполни эти три команды и запомни вывод:
  git log --oneline -5
  npx tsc --noEmit && echo TSC_OK
  find src -name '*.ts' | xargs wc -l | tail -1

ШАГ 3. Выполни эти два поиска:
  grep -rn "cache_read_input_tokens" src/
  grep -rn "acquireSessionWait" src/

ШАГ 4. Составь итоговый отчёт РОВНО из этих пяти разделов, в этом порядке:

  ## 1. Назначение проекта
  Два-три предложения: что делает этот сервис и зачем.

  ## 2. Путь запроса
  Перечисли по порядку, что происходит с входящим POST /v1/chat/completions:
  какие функции его обрабатывают и в каком файле каждая лежит.

  ## 3. Механика сессий
  Как устроено переиспользование сессии Claude CLI между запросами: где
  хранится соответствие, что происходит при параллельных запросах с одним
  ключом, что при перезапуске процесса.

  ## 4. Разбор делегирования
  Как текст ответа модели превращается в tool_calls: какие блоки
  игнорируются и почему, что происходит с неизвестным именем инструмента.

  ## 5. Факты из команд
  Приведи: последние 5 коммитов одной строкой каждый; прошёл ли tsc;
  суммарное число строк в src; сколько совпадений дал каждый из двух grep.

Отчёт — это твой финальный ответ. Не создавай файлов.
